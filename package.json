{
  "name": "apim-definitions",
  "version": "0.0.1",
  "description": "OAS (Swagger v3) API Definition for Patient Information",
  "scripts": {
    "test": "speccy lint patient-information-api.yaml",
    "publish": "mkdir -p dist && node_modules/.bin/speccy resolve patient-information-api.yaml -o dist/patient-information-api.yaml",
    "serve": "node_modules/.bin/speccy serve patient-information-api.yaml",
    "release": "mkdir -p public && cp dist/patient-information-api.yaml public/patient-information-api.yaml"
  },
  "author": "NHS Digital",
  "license": "ISC",
  "homepage": "https://bitbucket.org/laurencesmith/apim-definitions#readme",
  "dependencies": {
    "speccy": "^0.11.0"
  }
}
