{
  "name": "apim-definitions",
  "version": "0.0.1",
  "description": "OAS (Swagger v3) API Definition for Patient Information",
  "scripts": {
    "test": "speccy lint specification/patient-demographics-service-api.yaml",
    "publish": "mkdir -p dist && node_modules/.bin/speccy resolve specification/patient-demographics-service-api.yaml -i | poetry run python scripts/yaml2json.py | poetry run python scripts/set_version.py > dist/patient-demographics-service-api.json",
    "serve": "node_modules/.bin/speccy serve specification/patient-demographics-service-api.yaml",
    "check-licenses": "node_modules/.bin/license-checker --failOn GPL --failOn LGPL"
  },
  "author": "NHS Digital",
  "license": "MIT",
  "homepage": "https://bitbucket.org/laurencesmith/apim-definitions#readme",
  "dependencies": {
    "speccy": "^0.11.0"
  },
  "devDependencies": {
    "license-checker": "^25.0.1",
    "apigeetool": "^0.14.2"
  }
}
