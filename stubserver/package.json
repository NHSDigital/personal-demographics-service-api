{
  "name": "pds-api-stubserver",
  "version": "0.0.1",
  "description": "Patient Information API Stub/Mock Server",
  "main": "app.js",
  "scripts": {
    "start": "node app.js --use_strict",
    "deploy": "rm -rf dist/ && mkdir -p dist/mocks/ && cp *.js *.json *.yaml dist/ && cp -L mocks/*.json dist/mocks/ && apigeetool deployhostedtarget -e prod -o emea-demo8 --api Patient-Information --preserve-policies --import-only --directory dist/"
  },
  "author": "NHS Digital",
  "license": "ISC",
  "dependencies": {
    "@hapi/hapi": "^18.4.0",
    "@hapi/joi": "^16.1.8",
    "apigeetool": "^0.14.2",
    "inert": "^5.1.3",
    "nhs-number-validator": "^1.1.1"
  },
  "devDependencies": {
    "eslint": "^6.7.2"
  }
}
