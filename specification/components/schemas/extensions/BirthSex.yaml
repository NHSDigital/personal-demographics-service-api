type: object
description:
  Wrapper object for birth sex details. This is accepted in a create patient request body but will not be persisted to PDS.
required:
  - url
properties:
  url:
    type: string
    description: Definition of birth sex extension.
    example: https://fhir.hl7.org.uk/StructureDefinition/Extension-UKCore-BirthSex
    enum:
      - https://fhir.hl7.org.uk/StructureDefinition/Extension-UKCore-BirthSex
    readOnly: true
