type: array
description: List of addresses associated with the patient. This will only be fully populated on a retrieval, only a the `home` address will be returned on a search.
items:
  $ref: "Address.yaml"

