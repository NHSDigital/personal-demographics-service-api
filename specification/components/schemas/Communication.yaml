type: object
description: A language that may be used to communicate with the patient. This will only be populated on a retrieval and not a search.
properties:
  language:
    $ref: 'codeable/Language.yaml'
required:
  - language
