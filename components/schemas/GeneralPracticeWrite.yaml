type: object
required:
  - business_effective
properties:
  _id:
    type: string
    example: '123'
  _links:
    type: object
    properties:
      self:
        type: string
        format: url
        example: /Patient/1234567890/GeneralPractice/123
      organization:
        type: string
        format: url
        description: The location of the registered practice or practitioner resource
        example: /Organization/Y00001
  business_effective:
    type: object
    required:
      - start
    properties:
      start:
        description: A date in the format yyyy-mm-dd
        example: "2010-10-22"
        type: string
        format: date
      end:
        description: A date in the format yyyy-mm-dd
        example: "2010-10-22"
        type: string
        format: date
  practice_ods_code:
    description: The ODS code of the patient's general practice
    type: string
