type: object
properties:
  id:
    type: string
    example: '123'
  # _links: 
  #   type: object
  #   properties:
  #     self:
  #       type: string
  #       format: url
  #       description: The location of this related person resource
  #       example: /Patient/9000000009/RelatedPerson/1
  #     patient:
  #       description: The patient the person is related to
  #       type: string
  #       format: url
  #       example: /Patient/9000000009
  # TODO: do we need this on related person?
  # _version:
  #   type: string
  #   description: The NHS Digital assigned version of the patient resource
  #   example: "2"
  period:
    $ref: "DateRange.yaml"
  relationship: # TODO relationship type and role need investigating, see API notes
    type: array
    items:
      $ref: "PatientContactRelationship.yaml"
  name:
    $ref: "HumanName.yaml"
  telecom:
    type: array
    items:
      $ref: "ContactPoint.yaml"
  # TODO: model all of the below
  # ranking:
  #   type: integer
  #   example: 1
  # next_of_kin:
  #   type: boolean
  #   default: false
  # copy_correspondence:
  #   type: boolean
  #   default: false
  # callback_consent:
  #   type: boolean
  #   default: false
