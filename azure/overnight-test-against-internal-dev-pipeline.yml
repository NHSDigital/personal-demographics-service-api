name: "$(SourceBranchName)+$(BuildID)"

trigger: none
pr: none

schedules:
  - cron: 0 2 * * *
    displayName: 2am daily
    branches:
      include: ['master']
    always: true

resources:
  repositories:
    - repository: common
      type: github
      name: NHSDigital/api-management-utils
      ref: refs/heads/edge
      endpoint: NHSDigital

pool:
  name: 'AWS-ECS'

variables:
  - template: project.yml
  - name: environment
    value: internal-dev
  - name: SERVICE_ARTIFACT_NAME
    value: ''
  - name: FULLY_QUALIFIED_SERVICE_NAME
    value: personal-demographics-internal-dev   

steps:
  - bash: poetry install
    workingDirectory: $(Pipeline.Workspace)/s
    displayName: Setup tests
  
  - task: Npm@1
    displayName: Install node packages
    inputs:
      command: 'install'
      workingDir: '$(Pipeline.Workspace)/s'

  - template: azure/components/get-aws-secrets-and-ssm-params.yml@common
    parameters:
      secret_file_ids:
      - ${{ each secret_file_id in parameters.secret_file_ids }}:
          - ${{ secret_file_id }}
        secret_ids:
          - ptl/aazure-devops/apigee-nonprod/APIGEE_OTP_KEY
          - ptl/aazure-devops/apigee-nonprod/APIGEE_PASSWORD
          - ${{ each secret_id in parameters.secret_ids }}:
              - ${{ secret_id }}
        config_ids:
          - ptl/aazure-devops/apigee-nonprod/APIGEE_USERNAME
          - ${{ each config_id in parameters.config_ids }}:
              - ${{ config_id }}

  - template: azure/components/get-access-token.yml@common    

  - template: templates/pds-tests-karate.yml

  - template: templates/pds-tests.yml