
#docker_service_cpu: 256
#docker_service_memory: 512

docker_service:

  - name: pdsapi
    expose: true
    # TODO AMK need to implement the ping endpoint in the sandbox
    health_check:
      matcher: "200"
      path: "/_ping"
